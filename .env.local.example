# This is an example file. Create a .env.local file and add your Google Maps API key.
# Make sure the variable starts with NEXT_PUBLIC_ so it's available in the browser.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=YOUR_API_KEY_HERE
